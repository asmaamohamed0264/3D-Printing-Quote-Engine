services:
  quote-engine:
    build: .
    restart: unless-stopped

    expose:
      - "5000"

    environment:
      FLASK_ENV: production
      FLASK_DEBUG: "False"
      PRUSA_SLICER_PATH: /usr/bin/prusa-slicer
      SECRET_KEY: ${SECRET_KEY}

    volumes:
      - ./config.json:/app/config.json
      - ./logs:/app/logs

    # 🔥 TEMPORAR scoatem healthcheck complet
    # (după ce merge site-ul îl putem adăuga înapoi)

    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
